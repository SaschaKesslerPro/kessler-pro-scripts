/*!
 * kessler-pro-scripts / pdp.js
 * Product Detail Page — reserved for PDP-specific logic.
 * Currently empty: PDP scripts (judgemeinit, pdpcarouselheaderfix) are loaded via globals.js.
 * PDP CSS overrides also live in globals.js (loaded on every page, scoped via class selectors).
 */

(function () {
  'use strict';
  // PDP-specific logic goes here when needed.
})();
